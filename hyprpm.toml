[repository]
name = "horza"
authors = ["mehmed-ii"]

[horza]
description = "Workspace overview plugin for Hyprland."
authors = ["mehmed-ii"]
output = "build/libhorza.so"
build = [
  "cmake -S . -B build -DCMAKE_BUILD_TYPE=Release",
  "cmake --build build -j",
]
